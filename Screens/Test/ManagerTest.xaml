<UserControl x:Class="TestBuilder.Screens.Test.ManagerTest"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{ d:DesignInstance }">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock Text="Quản lý bộ đề"
                   TextAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="28"
                   FontWeight="SemiBold" />

        <StackPanel Grid.Row="1">
            <StackPanel Orientation="Horizontal" Margin="10">
                <TextBlock Text="Tên bộ đề" Margin="0 0 10 0" />
                <TextBox x:Name="TestName" MinWidth="210" />
            </StackPanel>
            <ListBox Height="100"
                     SelectionMode="Multiple"
                     x:Name="TestQuestionsListBox"
                     ItemsSource="{Binding}" />
            <Button x:Name="Button"
                    Content="Thêm bộ đề" 
                    Margin="10" 
                    HorizontalAlignment="Center" 
                    Padding="5 0"
                    Click="ButtonAdd"></Button>
        </StackPanel>

        <DataGrid Grid.Row="2"
                  x:Name="TestDataGrid"
                  ItemsSource="{Binding}"
                  AutoGenerateColumns="False"
                  MouseDoubleClick="TestDataGrid_Edit">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã đề" Width="Auto" Binding="{Binding TestId}" />
                <DataGridTextColumn Header="Tên đề" Width="*" Binding="{Binding Title}" />
                <DataGridTextColumn Header="Số lượng câu hỏi" Width="Auto" Binding="{Binding QuestionCount}" />
            </DataGrid.Columns>
            
            <DataGrid.CommandBindings>
                <CommandBinding Command="Delete" Executed="OnDelete"></CommandBinding>
            </DataGrid.CommandBindings>
        </DataGrid>
    </Grid>
</UserControl>