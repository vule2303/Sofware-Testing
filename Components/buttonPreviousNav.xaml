<UserControl x:Class="TestBuilder.View.UserControls.buttonPreviousNav"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="70" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70" />
        </Grid.RowDefinitions>
        <Grid Background="#fffffe">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="10*" />
                <ColumnDefinition Width="90*" />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Button Width="50" Height="50" Margin="5" Background="#6246ea" Content="Trở về" FontSize="14"
                    Foreground="#FFFFFFFE" FontWeight="Bold" />
            <TextBlock x:Name="LbHeading" Text="{Binding LabelHeading}"
                       Grid.Column="3" FontSize="24" Foreground="#2b2c34"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       FontWeight="Bold" />
        </Grid>
    </Grid>
</UserControl>